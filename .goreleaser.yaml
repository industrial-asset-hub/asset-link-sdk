# SPDX-FileCopyrightText: 2024 Siemens AG
#
# SPDX-License-Identifier: MIT
---
project_name: cdm-dcd-sdk

version: 2

before:
  hooks:
    - go generate ./...
    - go mod download

builds:
  - id: cdm-dcd-reference
    binary: cdm-dcd-reference
    main: ./cdm-dcd-reference/main.go
    tags:
      - webserver
    flags:
      - -trimpath
      - -mod=readonly
    ldflags:
      - -s -w -extldflags '-static'
      - -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.CommitDate}}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    mod_timestamp: "{{ .CommitTimestamp }}"
  - id: dcd-ctl
    binary: dcd-ctl
    main: ./cmd/dcd-ctl/dcd-ctl.go
    flags:
      - -trimpath
    ldflags:
      - -s -w -extldflags '-static'
      - -X github.com/industrial-asset-hub/asset-link-sdk/cmd/dcd-ctl/cmd.version={{.Version}}
        -X github.com/industrial-asset-hub/asset-link-sdk/cmd/dcd-ctl/cmd.commit={{.Commit}}
        -X github.com/industrial-asset-hub/asset-link-sdk/cmd/dcd-ctl/cmd.date={{.CommitDate}}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    mod_timestamp: "{{ .CommitTimestamp }}"
  - id: registry
    binary: registry
    main: ./registry/main.go
    flags:
      - -trimpath
    ldflags:
      - -s -w -extldflags '-static'
      - -X main.version={{.Version}}
        -X main.commit={{.Commit}}
        -X main.date={{.CommitDate}}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    mod_timestamp: "{{ .CommitTimestamp }}"

archives:
  - id: dcd-ctl
    builds:
      - dcd-ctl
    format: tar.gz
    name_template: >-
      {{  "dcd-ctl" }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip
    files: []

  - id: registry
    builds:
      - registry
    format: tar.gz
    name_template: >-
      {{  "registry" }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip
    files: []

  - id: cdm-dcd-reference
    builds:
      - cdm-dcd-reference
    format: tar.gz
    name_template: >-
      {{  "cdm-dcd-reference" }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip
    files: []

  - id: cdm-dcd-sdk
    meta: true
    format: tar.gz
    wrap_in_directory: false
    files:
      - README.SDK.md
      - cookiecutter-project-template/
    name_template: >-
      {{ .ProjectName }}

nfpms:
  - id: cdm-dcd-reference
    package_name: cdm-dcd-reference
    builds:
      - cdm-dcd-reference
    vendor: Siemens AG
    homepage: https://www.siemens.com/
    maintainer: Heiko Schabert <heiko.schabert@siemens.com>
    description: |-
      IAH Asset Link Reference Implementation.
    license: Siemens AG 2023
    formats:
      - deb
    bindir: /usr/bin
    version_metadata: git
    section: default
    priority: extra
    contents:
      - src: contrib/cdm-dcd-reference.service
        dst: /lib/systemd/system/cdm-dcd-reference.service
        type: "config"
    scripts:
      postinstall: "contrib/postinstall.bash"
      postremove: "contrib/postremove.bash"

dockers:
  - id: registry-image-linux-amd64
    ids:
      - registry
    image_templates:
      - "ghcr.io/industrial-asset-hub/asset-link-sdk/registry:{{ .Tag }}-linux-amd64"
    dockerfile: registry/Dockerfile
    use: buildx
    goos: linux
    goarch: amd64
    build_flag_templates:
      - "--platform=linux/amd64"
  - id: registry-image-linux-arm64
    ids:
      - registry
    image_templates:
      - "ghcr.io/industrial-asset-hub/asset-link-sdk/registry:{{ .Tag }}-linux-arm64"
    dockerfile: registry/Dockerfile
    use: buildx
    goos: linux
    goarch: amd64
    build_flag_templates:
      - "--platform=linux/arm64"

docker_manifests:
  - name_template: "ghcr.io/industrial-asset-hub/asset-link-sdk/registry:{{ .Tag }}"
    image_templates:
      - "ghcr.io/industrial-asset-hub/asset-link-sdk/registry:{{ .Tag }}-linux-amd64"
      - "ghcr.io/industrial-asset-hub/asset-link-sdk/registry:{{ .Tag }}-linux-arm64"

checksum:
  name_template: "{{.ProjectName}}_{{.Version}}.sha256"
  algorithm: sha256

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  groups:
    - title: Features
      regexp: "^.*feat[(\\w)]*:+.*$"
      order: 0
    - title: "Bug fixes"
      regexp: "^.*fix[(\\w)]*:+.*$"
      order: 1
    - title: Others
      order: 999
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "^Merge branch:"
